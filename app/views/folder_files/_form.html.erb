<%= form_with(model: folder_file, local: true) do |form| %>
  <% if folder_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(folder_file.errors.count, "error") %> prohibited this folder_file from being saved:</h2>

      <ul>
      <% folder_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
